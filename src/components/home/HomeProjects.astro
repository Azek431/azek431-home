---
type ProjectHighlight = {
  label: string;
  value: string;
};

type ProjectItem = {
  slug: string;
  title: string;
  description: string;
  longDescription: string;
  category: string;
  status: string;
  accent: string;
  tags: string[];
  highlights: ProjectHighlight[];
  websiteUrl?: string;
  githubUrl?: string;
  giteeUrl?: string;
};

type Props = {
  mainProject?: ProjectItem;
  sideProjects: ProjectItem[];
};

const { mainProject, sideProjects } = Astro.props as Props;

function externalProjectLinks(project: ProjectItem) {
  return [
    project.websiteUrl && {
      label: '访问项目',
      href: project.websiteUrl,
      primary: true,
    },
    project.githubUrl && {
      label: 'GitHub',
      href: project.githubUrl,
    },
    project.giteeUrl && {
      label: 'Gitee',
      href: project.giteeUrl,
    },
  ].filter(Boolean) as {
    label: string;
    href: string;
    primary?: boolean;
  }[];
}
---

<section class="home-v5-projects" aria-labelledby="home-projects-title">
  <header class="home-v5-section-head">
    <div>
      <p>FEATURED WORK</p>
      <h2 id="home-projects-title">不只展示技术栈，更展示正在生长的作品。</h2>
    </div>

    <a href="/projects/">
      查看全部项目
      <span aria-hidden="true">→</span>
    </a>
  </header>

  {
    mainProject && (
      <article class="home-v5-project-feature" style={`--project-accent: ${mainProject.accent};`}>
        <div class="home-v5-project-feature__preview" aria-hidden="true">
          <div class="home-v5-project-window">
            <div class="home-v5-project-window__bar">
              <span />
              <span />
              <span />
              <small>{mainProject.websiteUrl ?? mainProject.slug}</small>
            </div>

            <div class="home-v5-project-window__body">
              <aside>
                <strong>INDEX</strong>
                <span class="is-active" />
                <span />
                <span />
                <span />
              </aside>

              <div>
                <span class="home-v5-project-window__badge">{mainProject.category}</span>
                <strong>{mainProject.title}</strong>
                <i />
                <i />
                <i class="is-short" />

                <div class="home-v5-project-window__cards">
                  <span />
                  <span />
                  <span />
                </div>
              </div>
            </div>
          </div>

          <span class="home-v5-project-feature__glow" />
        </div>

        <div class="home-v5-project-feature__content">
          <div class="home-v5-project-feature__meta">
            <span>{mainProject.category}</span>
            <span>{mainProject.status}</span>
          </div>

          <h3>{mainProject.title}</h3>

          <p>{mainProject.longDescription || mainProject.description}</p>

          <dl class="home-v5-project-feature__stats">
            {mainProject.highlights.slice(0, 3).map((highlight) => (
              <div>
                <dt>{highlight.label}</dt>
                <dd>{highlight.value}</dd>
              </div>
            ))}
          </dl>

          <ul class="home-v5-project-feature__tags" aria-label="项目技术标签">
            {mainProject.tags.slice(0, 4).map((tag) => (
              <li>{tag}</li>
            ))}
          </ul>

          <div class="home-v5-project-feature__actions">
            <a
              class="home-v5-button home-v5-button--primary"
              href={`/projects/${mainProject.slug}/`}
            >
              项目详情
              <span aria-hidden="true">→</span>
            </a>

            {externalProjectLinks(mainProject).map((link) => (
              <a
                class="home-v5-project-feature__external"
                href={link.href}
                target="_blank"
                rel="noopener noreferrer"
              >
                {link.label}
                <span aria-hidden="true">↗</span>
              </a>
            ))}
          </div>
        </div>
      </article>
    )
  }

  <div class="home-v5-projects__secondary">
    {
      sideProjects.map((project, index) => (
        <article class="home-v5-project-card" style={`--project-accent: ${project.accent};`}>
          <div class="home-v5-project-card__top">
            <span>0{index + 2}</span>
            <div>
              <small>{project.category}</small>
              <strong>{project.status}</strong>
            </div>
          </div>

          <div class="home-v5-project-card__body">
            <h3>{project.title}</h3>
            <p>{project.description}</p>
          </div>

          <ul class="home-v5-project-card__tags" aria-label="项目标签">
            {project.tags.slice(0, 3).map((tag) => (
              <li>{tag}</li>
            ))}
          </ul>

          <div class="home-v5-project-card__footer">
            <a href={`/projects/${project.slug}/`}>
              查看详情
              <span aria-hidden="true">→</span>
            </a>

            <div>
              {project.githubUrl && (
                <a
                  href={project.githubUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  aria-label={`${project.title} GitHub 仓库`}
                >
                  GH
                </a>
              )}

              {project.giteeUrl && (
                <a
                  href={project.giteeUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  aria-label={`${project.title} Gitee 镜像`}
                >
                  GE
                </a>
              )}
            </div>
          </div>
        </article>
      ))
    }
  </div>
</section>
